name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: setup
      run: sudo apt remove gcc-arm-none-eabi && sudo apt-add-repository ppa:team-gcc-arm-embedded/ppa && sudo apt update && sudo apt-install gcc-arm-embedded device-tree-compiler
    - name: mkdir
      run: mkdir build 
    - name: configure
      run: cmake ../
      working-directory: build
    - name: make
      run: make
